"""NLP Intelligence System: from raw text to a production-grade ML service.

A reusable NLP pipeline (normalization, tokenization, stemming, lemmatization),
classical vectorizers (BoW, TF-IDF, BM25), word embeddings, a BM25 search engine,
and a Dockerized FastAPI service. Experiments tracked with MLflow, data versioned
with DVC.
"""

__version__ = "0.1.0"
